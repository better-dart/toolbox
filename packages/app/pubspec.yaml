name: toolbox
version: 1.0.0+1
publish_to: none
description: A new Flutter project.
environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  get: 4.6.3
  niku: ^1.1.3
  flutter_dotenv: ^5.0.2
  file_selector: ^0.8.2+1                    # open folder
  excel: ^2.0.0-null-safety-3
  syncfusion_flutter_xlsio: ^19.3.47-beta    # excel

dev_dependencies:
  build_runner: ^2.1.4
  flutter_lints: ^1.0.0
  get_cli: ^1.6.0
  dhttpd: ^4.0.0

  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

